# BenchDB documentation assets

This orphan branch stores generated product screenshots without adding binary
history to the application branch. The screenshots use BenchDB's deterministic
demo corpus and contain no production data.
